name: framed
description: "Framed — a local-area circular assassination game you play IRL."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  camera: ^0.12.0+1
  cryptography: ^2.9.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_localizations:
    sdk: flutter
  flutter_map: ^8.3.1
  freezed_annotation: ^3.1.0
  geolocator: ^14.0.3
  get_it: ^9.2.1
  latlong2: ^0.10.1
  mcp_toolkit: ^3.0.0
  mobile_scanner: ^7.2.0
  postgrest: ^2.8.0
  qr_flutter: ^4.1.0
  slang: ^4.18.0
  slang_flutter: ^4.18.0
  supabase_flutter: ^2.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  freezed: ^3.2.5
  build_runner: ^2.15.1
  slang_build_runner: ^4.18.0

flutter:
  uses-material-design: true
