<?xml version="1.0" encoding="utf-8"?>
<!-- Same as drawable/launch_background.xml — see the note there on why the
     splash stays charcoal regardless of the system's light/dark setting. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/brand_charcoal" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/launch_mark" />
    </item>
</layer-list>
